# Raspberry Pi 4b
BOARD_NAME="RPi4b"
BOARDFAMILY="bcm2711"
BOOTCONFIG="none"
KERNEL_TARGET="edge"

# too many hacks to make this work, move to a fragment where we use ubuntu's kernel and flash-kernel
add_fragment "raspi"
